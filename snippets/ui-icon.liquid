{% comment %}
  Renders an icon (Material Icons)

  Accepts:
  - name: {String} Icon name,
  - size: {Number} (optional) Icon size
  - filled: {String} (optional) Icon fill color
  - hidden: {Boolean} (optional) Hide icon

  Usage:
  {% render 'ui-icon', name: 'search', size: 24, filled: true, hidden: false %}
{% endcomment %}

<span
  ui-icon
  {% if filled %}
    filled
  {% endif %}
  {% if hidden %}
    hidden
  {% endif %}
  class="material-symbols-outlined"
  style="--icon-size: {{ size | default: 24 }}px;"
>
  {{ name }}
</span>
