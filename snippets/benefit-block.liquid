{% comment %}
  Renders a benefit item (Block)

  Accepts:
  - content: {String} Benefit content,
  - icon: {String} Benefit icon

  Usage:
  {% render 'benefit-block', content: 'Worldwide Shipping', icon: 'language' %}
{% endcomment %}

<li block="benefit">
  {% if icon %}
    {% render 'ui-icon', name: icon %}
  {% endif %}
  <span>{{ content }}</span>
</li>
