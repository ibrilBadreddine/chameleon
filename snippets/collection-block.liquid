{% comment %}
  Renders a collection item (Block)

  Accepts:
  - collection: {Object} Collection info
  - action: {String} Collection action button content

  Usage:
  {% render 'collections-block', collection: {...}, action: "View collection" %}
{% endcomment %}

<li block="collection">
  <div class="media">
    {% if collection.preview_image %}
      <img
        src="{{ collection.image }}"
        alt="{{ collection.name }}"
        height="100%"
        width="100%"
      >
    {% else %}
      {% render 'ui-icon', name: 'image', size: 60, filled: true %}
    {% endif %}
  </div>
  <div class="content">
    <div class="text">
      <h4 class="title">{{ collection.name | default: 'My collection' }}</h4>
      {% if collection.description %}
        <p class="description">{{ collection.description }}</p>
      {% endif %}
    </div>
    <a href="/collections/{{ collection.slug }}" ui-link="secondary" class="action">{{ action }}</a>
  </div>
</li>
