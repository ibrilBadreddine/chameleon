<div section="products">
  <div class="section-head">
    <h2>{{ section.settings.title }}</h2>
  </div>
  <ul class="section-core">
    {% for block in section.blocks %}
      {% render 'block-product', product: block.settings.product, badge: block.settings.badge, youcan_attributes: block.youcan_attributes %}
    {% endfor %}
  </ul>
</div>

{% schema %}
{
  "label": "Products",
  "templates": ["index", "product"],
  "limit": 3,
  "max_blocks": 24,
  "settings": [
    {
      "id": "title",
      "type": "text",
      "label": "t:common.content.title",
      "default": "Our Favorites"
    }
  ],
  "blocks": [
    {
      "type": "product",
      "label": "t:common.blocks.product",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "t:common.collection.label"
        },
        {
          "type": "text",
          "id": "badge",
          "label": "Badge"
        }
      ]
    }
  ]
}
{% endschema %}
